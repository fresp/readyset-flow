{
  "name": "readyset-review",
  "version": "0.2.0",
  "description": "Readyset — a standalone omp extension that fuses /plan's grounding discipline, a structured propose/review/apply workflow, and mattpocock/skills prompting hygiene into its own independent command, with no runtime dependency on any of the three.",
  "type": "module",
  "bin": {
    "readyset-review": "src/cli/install.mjs"
  },
  "files": [
    "src/lib",
    "src/extensions",
    "src/cli",
    "src/skill",
    "README.md"
  ],
  "scripts": {
    "test": "node --experimental-strip-types test/readyset-spec.test.mts && node --experimental-strip-types test/omp-config.test.mts && node --experimental-strip-types test/readyset-review.test.mts"
  },
  "keywords": [
    "omp",
    "oh-my-pi",
    "brainstorm",
    "spec-driven-development"
  ],
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18"
  }
}
